
name: Testing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      SCREEN_WIDTH: 1920
      SCREEN_HEIGHT: 1080
    container: elenium/standalone-chrome
    
    steps:
    - run: pwd
